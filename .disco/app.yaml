name: discolike
description: tt-discolike itself — the demo catalog page (dogfooding its own manifest convention)
port: 8760
hidden: true
launch: bash -c 'export DISCOLIKE_HOST=0.0.0.0; exec /home/ttuser/.tenstorrent-venv/bin/discolike'
